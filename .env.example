# Example environment variables for Django project
# Copy this file to .env and fill in your actual values

# Django Secret Key - Generate a new one for production
SECRET_KEY=your-secret-key-here

# Debug setting - Set to False for production
DEBUG=True

# For Azure deployment, you'll also need to set these in Azure App Service Configuration:
# - SECRET_KEY (same value as above)
# - DEBUG=False
# - DJANGO_SETTINGS_MODULE=mysite.settings (if not already set)

# Add other environment variables as needed
# DB_NAME=your-database-name
# DB_ENGINE=your-database-engine
